[Unit]
Description=Vector Service
After=network.target

[Service]
Type=simple
User={{ ansible_user_id }}
Group={{ ansible_user_gid }}
ExecStart=/usr/bin/vector --config-yaml {{ ansible_env.HOME }}/vector.yml
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
